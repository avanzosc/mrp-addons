<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="mrp.mrp_workorder_mrp_production_form" model="ir.actions.act_window">
        <field name="target">current</field>
    </record>

    <record id="mrp_production_workorder_form_view_inherit" model="ir.ui.view">
        <field name="model">mrp.workorder</field>
        <field name="inherit_id" ref="mrp.mrp_production_workorder_form_view_inherit"/>
        <field name="arch" type="xml">
            <header position="inside">
                <button name="button_start" type="object" string="Start" class="btn-success" invisible="production_state in ('draft', 'done') or working_state == 'blocked' or state == 'done' or is_user_working"/>
                <button name="button_pending" type="object" string="Pause" class="btn-warning" invisible="production_state in ('draft', 'done') or working_state == 'blocked' or not is_user_working"/>
                <button name="button_finish" type="object" string="Done" class="btn-success" invisible="production_state in ('draft', 'done') or working_state == 'blocked' or not is_user_working"/>
                <button name="%(mrp.act_mrp_block_workcenter_wo)d" type="action" string="Block" context="{'default_workcenter_id': workcenter_id}" class="btn-danger" invisible="production_state in ('draft', 'done') or working_state == 'blocked'"/>
                <button name="button_unblock" type="object" string="Unblock" context="{'default_workcenter_id': workcenter_id}" class="btn-danger" invisible="production_state in ('draft', 'done') or working_state != 'blocked'"/>
            </header>
        </field>
    </record>
</odoo>