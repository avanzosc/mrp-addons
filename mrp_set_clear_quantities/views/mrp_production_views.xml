<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="mrp_production_form_view" model="ir.ui.view">
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.mrp_production_form_view" />
        <field name="arch" type="xml">
            <field name="reservation_state" position="after">
                <field name="show_set_qty_button" />
                <field name="show_clear_qty_button" />
            </field>
            <button name="action_confirm" position="after">
                <button name="action_set_quantities_to_reservation"
                    attrs="{'invisible': [('show_set_qty_button', '=', False)]}"
                    string="Set quantities" type="object"
                    groups="stock.group_stock_user"
                    class="o_btn_validate" data-hotkey="g"/>
                <button name="action_clear_quantities_to_zero"
                    attrs="{'invisible': [('show_clear_qty_button', '=', False)]}"
                    string="Clear quantities" type="object"
                    groups="stock.group_stock_user"
                    class="o_btn_validate" data-hotkey="g"/>
            </button>
        </field>
    </record>
</odoo>
